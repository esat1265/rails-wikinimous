
<div class="container">
  <div class="row">
  <% @articles.each do |article| %>
    <div class="form-check">
        <h3><%= link_to article['title'], article_path(article["id"]) %></h3>
        <p><%= article['content']%></p>
    </div>
  <% end %>

  </div>
</div>
